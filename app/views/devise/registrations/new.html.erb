<div class = "sign_up_after">
  <h2> Three more seconds and you're in! </h2>
<!--  <br> 
  <%- if devise_mapping.omniauthable? %>
        <%- User.omniauth_providers.each do |provider| %>
        <%= link_to "Sign Up with #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider), :class => 'medium button twitter_btn' %><br />
        <% end -%>
      <% end -%>
  <br> -->
  <hr>
  <%= simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => 'form-vertical' }) do |f| %>
    <!-- Removing error name
    <%= f.error_notification %> 
    <%= display_base_errors resource %> 
    <%= f.input :username, :label => "Username", :autofocus => true %> -->
    <%= f.input :email, :label => "Confirm your E-mail", :required => true %>
    <% if f.object.password_required? %>
    	<%= f.input :password, :required => true %>
    	<%= f.input :password_confirmation, :required => true %>
    <% end %>
    <%= f.button :submit, 'Enter', :class => 'button' %>
  <% end %>
</div>





